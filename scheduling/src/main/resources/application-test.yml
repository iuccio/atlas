spring:
  datasource:
    url: jdbc:postgresql://scheduling-test.cdv0rjtmldns.eu-central-1.rds.amazonaws.com:5432/scheduling
    username: ${DB_USER}
    password: ${DB_PWD}
  kafka:
    atlas:
      mail:
        topic: atlas.test.mail
        groupId: atlas.kafka.user
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://login.microsoftonline.com/2cda5d11-f0ac-46b3-967d-af1b2e1bd01a/v2.0
      client:
        provider:
          azure:
            issuer-uri: https://login.microsoftonline.com/2cda5d11-f0ac-46b3-967d-af1b2e1bd01a/v2.0
        registration:
          azure:
            authorization-grant-type: client_credentials
            client-id: ${SCHEDULING_CLIENT_ID}
            client-secret: ${SCHEDULING_CLIENT_SECRET}
            scope:
              - api://2e8c6ce0-304d-42e1-9bce-600b3ea43aad/.default
auth:
  audience:
    service-name: 2e8c6ce0-304d-42e1-9bce-600b3ea43aad
logging:
  pattern:
    console: timestamp=%d thread=%t loglevel=%-5p class=%c appname=atlas traceid=%X{traceId} spanid=%X{spanId} message="%m"%n
atlas:
  client:
    gateway:
      url: "http://gateway:8888"