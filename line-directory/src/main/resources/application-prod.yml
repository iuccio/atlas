spring:
  datasource:
    url: jdbc:postgresql://line-directory-prod.czdfgxj0x1zx.eu-central-1.rds.amazonaws.com/line_directory
    username: ${DB_USER}
    password: ${DB_PWD}
auth:
  audience:
    service-name: f3cdcb3e-1e95-4591-b664-4526d00f5d66
logging:
  pattern:
    console: timestamp=%d thread=%t loglevel=%-5p class=%c appname=atlas traceid=%X{traceId} spanid=%X{spanId} message="%m"%n
amazon:
  accessKey: ${AMAZON_S3_ACCESS_KEY}
  secretKey: ${AMAZON_S3_SECRET_KEY}
  region: "eu-central-1"
  bucket:
    name: "atlas-data-export-prod"