import { MainlineSelectOptionPipe } from './mainline-select-option.pipe';

describe('MainlineSelectOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new MainlineSelectOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
