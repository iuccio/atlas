import { EditTitlePipe } from './edit-title.pipe';

describe('EditTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new EditTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
