import { MouseOverTitleDirective } from './mouse-over-title.directive';

describe('MouseOverTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseOverTitleDirective();
    expect(directive).toBeTruthy();
  });
});
