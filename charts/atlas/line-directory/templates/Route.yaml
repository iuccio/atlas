kind: Route
apiVersion: route.openshift.io/v1
metadata:
  annotations:
    openshift.io/host.generated: 'true'
    clew.sbb.ch/cluster-independent-dns: 'true'
  name: '{{ .Values.product }}{{ .Values.global.separator }}{{ .Values.global.variant }}'
spec:
  host: '{{ .Values.product }}.{{ .Values.global.springProfilesActive }}.sbb-cloud.net'
  port:
    targetPort: '{{ .Values.port }}-tcp'
  to:
    kind: Service
    name: '{{ .Values.product }}{{ .Values.global.separator }}{{ .Values.global.variant }}'
    weight: 100
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Allow
