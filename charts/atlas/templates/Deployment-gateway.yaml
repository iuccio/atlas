kind: Deployment
apiVersion: apps/v1
metadata:
  name: '{{ .Values.gateway.name }}'
spec:
  replicas: {{ .Values.common.replicas }}
  selector:
    matchLabels:
      app: '{{ .Values.gateway.name }}'
      deploymentconfig: '{{ .Values.gateway.name }}'
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      annotations:
        collectord.io/logs-index: '{{ .Values.common.splunkIndex }}'
        collectord.io/logs-output: splunk::prod
        collectord.io/logs-eventpattern: ^(timestamp=)
      labels:
        app: '{{ .Values.gateway.name }}'
        deploymentconfig: '{{ .Values.gateway.name }}'
    spec:
      containers:
      - env:
        - name: SPRING_PROFILES_ACTIVE
          value: '{{ .Values.common.springProfilesActive }}'
        image: "atlas.docker.bin.sbb.ch/{{ .Values.gateway.name }}:{{ .Chart.AppVersion }}"
        imagePullPolicy: '{{ .Values.common.imagePullPolicy }}'
        name: '{{ .Values.gateway.name }}'
        ports:
        - containerPort: {{ .Values.gateway.port }}
          protocol: TCP
        resources:
          requests:
            cpu: '{{ .Values.common.cpuMin }}'
            memory: '{{ .Values.common.memoryMin }}'
          limits:
            cpu: '{{ .Values.common.cpuMax }}'
            memory: '{{ .Values.common.memoryMax }}'
        livenessProbe:
          httpGet:
            path: '{{ .Values.gateway.livenessProbe }}'
            port: {{ .Values.gateway.port }}
            scheme: HTTP
          initialDelaySeconds: 40
          timeoutSeconds: 90
          periodSeconds: 20
          successThreshold: 1
          failureThreshold: 10
        readinessProbe:
          httpGet:
            path: '{{ .Values.gateway.readynessProbe }}'
            port: {{ .Values.gateway.port }}
            scheme: HTTP
          initialDelaySeconds: 30
          timeoutSeconds: 90
          periodSeconds: 20
          successThreshold: 1
          failureThreshold: 10
        terminationMessagePath: /dev/termination-log
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
