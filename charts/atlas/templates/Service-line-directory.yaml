kind: Service
apiVersion: v1
metadata:
  name: '{{ .Values.lineDirectory.name }}'
spec:
  ports:
  - name: '{{ .Values.lineDirectory.port }}-tcp'
    port: {{ .Values.lineDirectory.port }}
    protocol: TCP
    targetPort: {{ .Values.lineDirectory.port }}
  selector:
    app: '{{ .Values.lineDirectory.name }}'
    deploymentconfig: '{{ .Values.lineDirectory.name }}'
  sessionAffinity: None
  type: ClusterIP
