kind: Route
apiVersion: route.openshift.io/v1
metadata:
  annotations:
    openshift.io/host.generated: 'true'
    clew.sbb.ch/cluster-independent-dns: 'true'
  name: '{{ .Values.lineDirectory.name }}'
spec:
  host: '{{ .Values.lineDirectory.name }}.{{ .Values.common.springProfilesActive }}.sbb-cloud.net'
  port:
    targetPort: '{{ .Values.lineDirectory.port }}-tcp'
  to:
    kind: Service
    name: '{{ .Values.lineDirectory.name }}'
    weight: 100
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Allow
